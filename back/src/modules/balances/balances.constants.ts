/**
 * Constantes de la consulta de balances.
 *
 * El diccionario de conceptos —qué código representa el activo en cada
 * formulario— vive en `common/finanzas/conceptos.ts`, compartido con los
 * importadores para que no puedan discrepar.
 */

/** Formulario IFRS de 622 cuentas. Es el plan de referencia de la aplicación. */
export const FORMULARIO_IFRS = 1;
